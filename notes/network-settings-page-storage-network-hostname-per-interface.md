# *Network settings page** Storage → Network: hostname, per-interface DHCP/static address, gateway, DNS; a change is applied with a countdown and reverts unless the page confirms it still reaches the NAS; shows the mDNS name to type on other machines

- 2026-09-14 15:01 — Already shipped in 0530aed (NAS mode: the Network page — the box's name and address, with a revert); closed 2026-09-14 when the board was checked against the code.
