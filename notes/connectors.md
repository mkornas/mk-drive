# Connectors

- 2026-09-11 17:10 — Shipped 3730567, deployed. WebDAV + S3 providers (pure Node: fetch + tolerant multistatus parser; SigV4 by hand), Staging for chunked uploads, Connectors registry (SQLite , tried before saved, secrets never returned), Locations.add/remove, Location.source, admin routes /api/connectors, Settings → Locations page (mounts + connectors, add form, remove). Trash on a connector = .mk-drive/trash on the remote; a remote that refuses it (another mk-drive) makes delete fail with a clear 409 instead of deleting for good. SMB stays a host mount (no maintained Node client). Tests: a second drive as WebDAV remote + a fake S3 (64 total). Verified live: a WebDAV connector to the drive's own /dav/Drive/ added through the UI, listed its folders in the sidebar; removed afterwards. — main @ 3730567 Connectors: a WebDAV server or an S3 bucket as a location
