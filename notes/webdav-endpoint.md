# WebDAV endpoint

- 2026-09-11 11:01 — Shipped 236887c, deployed. routes/dav.ts: /dav/ root lists the caller's locations; PROPFIND (depth 0/1, quota on location roots), GET/HEAD with ranges + ETags, PUT (any or no content type, streams to provider.write), MKCOL, COPY/MOVE (Destination on this drive, Overwrite F→412), DELETE → trash, LOCK/UNLOCK advisory (LOCK on a new name creates the empty file Finder expects), PROPPATCH answers 403 per prop. Non-standard methods via Fastify addHttpMethod; an encapsulated plugin with a catch-all body parser. Auth hook challenges /dav with Basic; OPTIONS open. 7 tests (55 total). Verified live with curl against a running drive: OPTIONS, PROPFIND, PUT 201, GET, MOVE, DELETE 204, LOCK token; probes purged, token removed. Also docs/GUIDE.md and README links. — main @ 236887c WebDAV at /dav/: the drive in Finder, Explorer and Files
