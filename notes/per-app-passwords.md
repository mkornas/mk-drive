# Per-app passwords

- 2026-09-11 10:50 — Shipped f7e2d5e, deployed. app_passwords table (sha256 of a 30-byte secret, prefix for display, last use + ip at most once a minute); auth.ts maps Authorization: Basic email:secret or Bearer secret to the user (via 'token'), throttled per address, WWW-Authenticate on 401; account routes (logout, sessions, password, name), the admin pages and the app-password routes themselves refuse a token (sessionOnly). Devices page: make (shown once with a curl hint), list, remove. README section. 6 tests. Verified live with curl -u and Bearer against a running drive; token removed afterwards. On the way: fixed the SSO button (service worker served index.html for /auth/login, 0e24c98) and stopped board/docs commits from redeploying (815c166). — main @ f7e2d5e App passwords for non-browser clients
