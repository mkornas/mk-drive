# *Password sign-in can be held shut from outside** failed logins throttle email:<address> for up to 30 s, so anyone who knows an address keeps its password form blocked through the tunnel (SSO unaffected); no throttle avoids this without letting many addresses guess one account faster. Recommend password sign-in = local when a tunnel and SSO are set up: say so on Settings → Sign-in and in the guide, maybe suggest it when the tunnel is turned on

- 2026-09-18 12:15 — No throttle change: any key that stops a pool of addresses guessing one account lets one address keep it paused, and the other way round. Instead the recommendation is in three places, on the branch sign-in-lockout (6895efb): the Everywhere text on Settings → Sign-in (only while single sign-on is on), a note on the Storage → Network tunnel card when a tunnel is configured, passwords work everywhere and single sign-on is on (linking to Settings → Sign-in), and the guide's 3.3. Client builds, its tests pass. Not merged. — sign-in-lockout @ 6895efb Sign-in: say when passwords everywhere can be held shut from outside, and where the tunnel is
